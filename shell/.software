#!/usr/bin/env zsh
# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
# Custom comment, If don't want conda active automatically,
# exec: conda config --set auto_activate_base false
__conda_setup="$('/home/will/miniconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/home/will/miniconda3/etc/profile.d/conda.sh" ]; then
        . "/home/will/miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="/home/will/miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<

# <<< Go Version Manager config <<<
[[ -s "/home/will/.gvm/scripts/gvm" ]] && source "/home/will/.gvm/scripts/gvm"

# <<< ROS config <<<
[[ -s "/opt/ros/noetic/setup.zsh" ]] && source "/opt/ros/noetic/setup.zsh"
